# ─── Michael Voice Agent — Netlify Environment Variables ───

# OpenAI (for call server — set in Railway too)
OPENAI_API_KEY=sk-...

# Anthropic (for debrief generation)
ANTHROPIC_API_KEY=sk-ant-...

# ElevenLabs (for call server — set in Railway too)
ELEVENLABS_API_KEY=...

# Call Server (Railway deployment URL)
CALL_SERVER_URL=https://your-app.up.railway.app
CALL_SERVER_SECRET=generate_a_random_32_char_secret
